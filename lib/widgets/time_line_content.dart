import 'package:flutter/material.dart';

class TimeLineContent extends StatelessWidget {
  const TimeLineContent({Key? key, required this.body}) : super(key: key);

  final Widget body;

  @override
  Widget build(BuildContext context) {
    return body;
  }
}
